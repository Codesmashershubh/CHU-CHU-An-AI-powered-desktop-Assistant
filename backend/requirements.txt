# Chu Chu backend — dependencies kept deliberately lean.
# Render's free instance has 512MB RAM / 0.1 CPU, so every package here earns its place.
# No ML/inference libraries (torch, whisper, transformers) ship server-side — the AI
# engine calls free hosted APIs (Groq / Gemini / Tavily) over plain HTTP instead of
# running models in-process. See docs/ARCHITECTURE.md for the reasoning.

fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
pydantic==2.10.4
pydantic-settings==2.7.1
sqlalchemy[asyncio]==2.0.36
aiosqlite==0.20.0
asyncpg==0.30.0
python-multipart==0.0.20
beautifulsoup4==4.12.3
python-dotenv==1.0.1
itsdangerous==2.2.0
